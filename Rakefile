#!/usr/bin/env rake


require File.expand_path('../config/application', __FILE__)

RubyRings::Application.load_tasks

require 'adhearsion/tasks'
#require 'rubygems'
#require 'bundler'
#Bundler.setup
#Bundler.require

